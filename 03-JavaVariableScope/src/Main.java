public class Main {
    public static void main(String[] args) {

        //local - declared inside a method, and it's visible only in that method

        //global - declared outside a method, but within a class, and it's visible to all parts of the class

        DiceRoller diceRoller = new DiceRoller();
    }
}
